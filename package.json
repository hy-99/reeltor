{
  "name": "reeltor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "worker": "npm run build && node dist/src/workers/process-reels.js",
    "worker:test": "npm run build && node dist/scripts/test-single.js",
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "@ffprobe-installer/ffprobe": "^2.1.2",
    "dotenv": "^17.3.1",
    "pg": "^8.16.3",
    "pino": "^10.1.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "@types/pg": "^8.20.0",
    "typescript": "^5.9.3"
  }
}
